fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
pydantic>=2.10,<3
python-dotenv>=1.0,<2
boto3>=1.35,<2
python-pptx>=1.0,<2
httpx>=0.28,<0.29
openai>=1.58,<2

# 음성 분석 (app/audio/delivery_metrics.py) — 그룹A 필러 VAD, WPM, 성량변화
# pip install만으로 가중치까지 다 받아짐 — GitHub 클론/torch.hub 다운로드 불필요.
# m4a/webm 등 압축 오디오 디코딩에는 시스템에 ffmpeg가 설치되어 있어야 함(librosa가 내부 사용).
numpy>=1.26,<2
silero-vad>=5.1,<6
torch>=2.2,<3
librosa>=0.10,<0.11
soundfile>=0.12,<0.13

# 테스트
pytest>=8.3,<9
